# 推論服務的 Pipelining: SGLang、vLLM

前五章的 pipeline 都在「訓練 / 單次計算」的世界。 最後一章走到 **LLM 推論服務**: 這裡的 stage 是「CPU 排程」與「GPU forward」、是「prefill」與「decode」。 SGLang、vLLM 這些引擎的高吞吐, 很大一部分就是把這些 stage 重疊起來 — 本 track 的所有 idea 在這裡收束。

## Continuous Batching

傳統 static batching 要湊滿一個 batch、等它整批跑完才收下一批 — 但 LLM 每個 request 的輸出長度差異極大, 短的早就結束卻得等最長的那個, GPU 大量空轉。 **Continuous batching (in-flight batching)** 改成 **以 token step 為單位**動態調整 batch: 每個 decode step 結束後, 已完成的 request 立刻離開、等待中的 request 立刻補進來。

這本身就是一種 pipeline 思維: 把「request 生命週期」解耦成一個個 step, 讓 batch 這個「工作站」永遠保持滿載, 而不是被最慢的 request 卡住 (回想第 1 章的 stage 平衡與木桶效應)。

<Callout variant="insight" title="落到真實場景: chatbot 混長短 request">
這在真實 chatbot 服務裡差距是數量級的: 一個 batch 混著「幫我改個錯字」(輸出 10 token) 和「寫一篇長文」(輸出 2000 token) 的 request, static batching 會讓那些短 request 佔著 slot 空轉到最長的跑完。 vLLM/SGLang 的 continuous batching 讓短 request 一結束就把 slot 讓給佇列裡的新 request, 實測 throughput 相對 static batching 常有數倍提升。 這也是為什麼線上 serving 幾乎不用 static batching。
</Callout>

<Exercise id="sp6-e1" title="Continuous vs static batching" kind="paper" level="進階">
一個 batch 有 8 個 request, 輸出長度分別是 10, 10, 10, 10, 500, 500, 500, 500 個 token。 假設每個 decode step 對整個 batch 花固定時間。 (a) static batching 下, 前 4 個短 request 完成後到整批結束, 有多少 step 它們的 slot 是浪費的? (b) continuous batching 如何回收這些 slot? (c) 這對整體 throughput (tokens/sec) 的影響?

<Solution>
(a) 短 request 在 step 10 就完成, 但 batch 要跑到 step 500。 之後的 `500 − 10 = 490` 個 step, 這 4 個 slot 都在算已結束的 request (padding / 空轉) — 浪費約 `490 × 4` 個 slot-step。

(b) Continuous batching 在短 request 於 step 10 完成後, 立刻把它們的 slot 讓給佇列中等待的新 request, batch 維持滿載。

(c) 有效 batch size 維持高檔, GPU 的每個 forward 都在算「真的有用的」token, throughput (tokens/sec) 大幅提升 — 這是現代 LLM serving 的基礎優化, vLLM、SGLang、TensorRT-LLM 皆內建。
</Solution>
</Exercise>

## Overlap Scheduler: CPU 排程疊 GPU Forward

每個 decode step 之間, CPU 要做不少事: 決定下一個 batch 有哪些 request、查 radix cache (prefix 共用)、準備 attention 的 metadata、處理上一步的 output token、sampling。 樸素做法讓 GPU 等 CPU 做完這些才跑下一步 — GPU 在 CPU 忙的時候空轉。

SGLang 的 **zero-overhead / overlap scheduler** 把這變成一個 2-stage pipeline: 讓 scheduler **領先一個 batch**, 在 GPU 正在跑 batch `n` 時, CPU 已經在準備 batch `n+1` 的所有 metadata。

<OverlapSchedulerFigure />

這跟第 3 章的 host–device stream overlap 是**同一個 idea**, 只是 stage 從「H2D copy vs. kernel」換成「CPU 排程 vs. GPU forward」。 stage 用不同資源 (CPU core vs. SM), 重疊就等於把 CPU 開銷藏進 GPU 的 forward 時間。

```python
# Conceptual overlap-scheduler loop (SGLang-style). The scheduler prepares batch
# n+1 on the CPU while the GPU still runs batch n; results are resolved one step
# later. Real implementations run the scheduler in a dedicated process and use a
# future/result queue instead of a blocking get.
next_batch = scheduler.prepare(get_requests())      # metadata for batch 0
result = None
while running:
    cur = next_batch
    gpu_future = model.forward_async(cur)           # launch GPU work, don't block
    next_batch = scheduler.prepare(get_requests())  # CPU builds batch n+1 meanwhile
    tokens = gpu_future.result()                    # now collect batch n's output
    scheduler.process_output(cur, tokens)           # detokenize / update cache
```

實務上 SGLang 用 `--enable-overlap-schedule` (新版預設開啟) 把 scheduler 放在獨立 process, 用 result queue 把 GPU 結果延後一個 step 取回。 這樣做的前提是排程與 forward 之間沒有硬依賴需要立刻解決 — 大多數 decode step 成立。

<Callout variant="insight" title="落到真實場景: decode 小 batch 時 CPU 開銷最傷">
這在 decode-heavy 服務最關鍵: 小 batch 的 decode step, GPU forward 可能只要 5–8ms, 而 CPU 端要查 radix cache、建 attention metadata、sample、detokenize, 輕鬆吃掉 3ms 以上。 不重疊時 GPU 每 step 有近三成時間在等 CPU (見 sp6-e2); SGLang 的 `--enable-overlap-schedule` 把 scheduler 放獨立 process、領先一個 batch, 就把這 3ms 藏進 GPU 的 forward, GPU 使用率逼近 100%。 反過來, 大 batch prefill 時 GPU forward 幾十 ms, CPU 那 3ms 佔比很小, overlap 的收益就有限 — 收益大小完全取決於 CPU/GPU 的時間比。
</Callout>

<Exercise id="sp6-e2" title="overlap scheduler 的收益上限" kind="paper" level="挑戰">
某 decode step: GPU forward 8ms, CPU 排程 3ms。 (a) 不重疊時每 step 多久? GPU 利用率? (b) 完美重疊後每 step 多久? (c) 若換成很大的 batch 讓 GPU forward 變 40ms, 而 CPU 仍 3ms, overlap scheduler 還重要嗎? (d) 反過來, 什麼情境 overlap scheduler 最關鍵?

<Solution>
(a) 不重疊: `8 + 3 = 11ms`/step, GPU 利用率 `8/11 ≈ 73%` (CPU 忙的 3ms GPU 空轉)。

(b) 完美重疊: CPU 的 3ms 藏進 GPU 的 8ms, 每 step ≈ `max(8, 3) = 8ms` → 利用率逼近 100%, throughput 提升約 `11/8 ≈ 1.375×`。

(c) 大 batch 時 `max(40, 3) = 40` vs `43` — 只省 ~7%, 相對不重要 (GPU 本來就忙, CPU 佔比小)。

(d) **小 batch / 短 sequence / 高 QPS 的 decode**: GPU 每 step 很快跑完, CPU 排程開銷佔比大, 這時把 CPU 藏進 GPU 收益最大。 這也解釋為何 decode-heavy 的 serving 特別在意 CPU overhead。
</Solution>
</Exercise>

## Chunked Prefill 與 Prefill/Decode 的 Pipeline

LLM 推論有兩個性質迥異的階段: **prefill** (一次處理整個 prompt, compute-bound、算很久) 與 **decode** (一次一個 token, memory-bound、很快但很多步)。 把一個長 prompt 的 prefill 整包塞進一個 forward, 會**卡住**同 batch 裡其他 request 的 decode (延遲飆高)。

**Chunked prefill** 把長 prompt 的 prefill 切成小 chunk, 讓每個 forward 只做一塊 prefill, 並可與其他 request 的 decode **混在同一個 batch** (SGLang 的 `--enable-mixed-chunk`)。 於是 prefill 與 decode 這兩個 stage 在時間上被打散、交錯, 避免長 prefill 造成的 decode 卡頓 (head-of-line blocking)。

<Callout variant="insight" title="落到真實場景: 長文件貼進來時別讓別人的回覆凍住">
這是長 context 服務的必備開關: 一個使用者貼了 8000-token 的文件進來 prefill, 若不切, 同 batch 其他人正在 stream 的回覆會突然卡頓 (inter-token latency 從 5ms 跳到幾十甚至上百 ms), 使用者體感就是「打字打到一半凍住」。 SGLang 的 `--chunked-prefill-size` (vLLM 也有對應設定) 把長 prefill 切塊與 decode 混批, 換來平順的 ITL, 代價是那個長 prompt 的首 token 延遲 (TTFT) 略增。 調這顆旋鈕就是在 TTFT 與 ITL 之間選一個服務要的平衡點。
</Callout>

<Exercise id="sp6-e3" title="為什麼要切 prefill" kind="paper" level="挑戰">
一個 batch 裡有 1 個 4096-token 的 prompt 要 prefill, 還有 30 個 request 正在 decode。 假設整包 prefill 一個 forward 要 80ms, 而純 decode step 只要 5ms。 (a) 不切 prefill, 那 30 個 decode request 這一步要等多久? (b) 把 prefill 切成 8 個 chunk (每 chunk ~10ms) 並與 decode 混合, 對 decode 的延遲有何改善? (c) 代價是什麼?

<Solution>
(a) 不切: 這一步要跑完 80ms 的 prefill, 30 個 decode request 全部被卡 80ms — inter-token latency 從 5ms 暴增到 80ms (head-of-line blocking)。

(b) 切成 8 個 ~10ms 的 chunk 並與 decode 混批: 每個 forward 只夾一小塊 prefill + decode, 單步約 10–15ms, decode 的 inter-token latency 從 80ms 降到十幾 ms 且平穩。 prefill 用多步逐塊完成。

(c) 代價: prefill 的**總完成時間**略增 (被切開、每塊有固定 overhead, 且與 decode 搶資源), 且排程更複雜。 這是典型的 latency (decode 平順) vs. throughput (prefill 盡快完成) 取捨, 由 `--chunked-prefill-size` 調。
</Solution>
</Exercise>

## Prefill/Decode Disaggregation (延伸)

再往前一步, 有些系統 (SGLang PD disaggregation、vLLM 的相關功能) 把 prefill 與 decode **拆到不同 GPU/節點**: prefill GPU 專做 compute-bound 的 prompt 處理, 算完把 KV cache 傳給 decode GPU 專做 memory-bound 的逐 token 生成。 這是把「prefill vs decode」這兩個 stage 從時間上的重疊, 升級成空間上的**專用化 pipeline**, 各自用最適合的 batch 策略與硬體, 代價是要在兩者間搬 KV cache。

<Callout variant="insight" title="落到真實場景: PD 分離的真實部署長相">
真實部署長這樣: DeepSeek 與 SGLang 的 PD disaggregation 把一組 GPU 專門當 prefill node (追求 prompt 吞吐、大 GEMM 吃滿 compute), 另一組當 decode node (大 batch continuous batching 攤銷 KV/權重讀取), 中間用高速網路 (甚至 RDMA/NVLink) 把 KV cache 從 prefill 搬到 decode。 好處是兩種性質相反的 workload 不再互相干擾, 各自用最適配置與 batch 策略; 代價是每個 request 要多一次跨節點 KV 傳輸與更複雜的路由。 通常規模夠大、prefill 與 decode 資源比例失衡時才划算。
</Callout>

<Exercise id="sp6-e4" title="PD 分離的動機" kind="paper" level="挑戰">
(a) prefill 與 decode 各是 compute-bound 還是 memory-bound? (b) 兩者混在同一 GPU 上為什麼難同時最佳化? (c) 分離後各自能怎麼調 batch? (d) 分離的主要成本是什麼?

<Solution>
(a) Prefill: 一次處理整個 prompt 的所有 token, 算術密度高 → **compute-bound**。 Decode: 一次一個 token, 主要成本是讀整個 KV cache 與權重 → **memory-bound**。

(b) 兩者最佳的 batch 組成與 kernel 配置不同 (prefill 要大 GEMM、decode 要高 batch 攤 KV 讀取); 混在一起互相干擾, 長 prefill 卡 decode (見 sp6-e3), 難同時把兩種 workload 都推到最佳。

(c) 分離後: prefill GPU 用適合 compute-bound 的配置追求 prompt 吞吐; decode GPU 用大 batch continuous batching 攤銷 memory 讀取、追求 tokens/sec。

(d) 成本: 要把 prefill 產生的 **KV cache 從 prefill GPU 搬到 decode GPU** (跨 GPU/節點傳輸), 以及更複雜的排程與路由。 當 prefill 與 decode 的資源需求差異夠大、規模夠大時才划算。
</Solution>
</Exercise>

## 產業推論引擎怎麼實作 overlap: TensorRT-LLM 與 AMD 生態

前面的 continuous batching、overlap scheduler、chunked prefill 不是 SGLang 專屬 — 它們是現代推論引擎的共同語言。 NVIDIA 與 AMD 各自把這套 idea 包進自己的生產堆疊。

### NVIDIA TensorRT-LLM

TensorRT-LLM 是 NVIDIA 的 LLM 推論函式庫, 把同一套重疊思路落在幾個地方:

- **In-flight batching (IFB)**: 就是 continuous batching 的 NVIDIA 命名 — 由 executor / batch manager 以 token step 為單位動態進出 request, 讓已完成的立刻離開、等待的立刻補進 (對應本章第一節)。
- **Chunked context (chunked prefill)**: 把長 prompt 的 context 階段切塊, 與 generation (decode) 混批, 避免長 prefill 卡住 decode — 與 SGLang 的 `--enable-mixed-chunk` 同構。
- **Overlap + CUDA Graph**: scheduler / sampling 等 CPU 工作與 GPU forward 重疊 (對應 overlap scheduler), 並用 **CUDA Graph** 把 decode step 的大量小 kernel launch 收成一次 replay, 消掉 launch overhead (呼應第 3 章的 host–device pipeline)。 底層 GEMM/attention kernel 大量來自 **CUTLASS** — 也就是說 sp2 的 kernel pipeline 就活在這裡面。

### AMD 生態: AITER + vLLM/SGLang on ROCm

AMD 這邊不是單一引擎, 而是**同一批開源引擎跑在 ROCm 上、換上 AMD 的 kernel**:

- **引擎層**: vLLM 與 SGLang 都有 ROCm 支援, continuous batching、overlap scheduler、chunked prefill 這些 serving 層邏輯**不變** — 因為它們是框架邏輯, 與廠牌無關。
- **kernel 層**: 把底下的 attention / GEMM / MoE 算子換成 **AITER** 提供的 ROCm 版本 (backend 是 sp2 提到的 CK / Triton / 手調 ASM), 在 MI300X 等硬體上取得高吞吐。
- **graph 層**: 用 **hipGraph** (CUDA Graph 的 ROCm 對應) 消 decode 的 launch overhead。

<Callout variant="insight" title="serving 層與廠牌無關, kernel 層才分家">
Continuous batching / overlap scheduler / chunked prefill 是**排程層**的 idea, NVIDIA 與 AMD 完全共用 — TensorRT-LLM、vLLM、SGLang 講的是同一件事。 真正分家的是**底層 kernel** (CUTLASS vs. AITER/CK) 與 **graph API** (CUDA Graph vs. hipGraph)。 換平台時 serving 邏輯照搬, 換掉的是 kernel 後端。
</Callout>

| 層級 | NVIDIA | AMD (ROCm) |
| --- | --- | --- |
| 推論引擎 | TensorRT-LLM / vLLM / SGLang | vLLM / SGLang on ROCm |
| Continuous batching | in-flight batching (IFB) | 同 (框架邏輯共用) |
| Chunked prefill | chunked context | 同 (框架邏輯共用) |
| 消 launch overhead | CUDA Graph | hipGraph |
| 底層 kernel | CUTLASS / cuBLAS / TRT plugins | AITER (CK / Triton / ASM) |

<Exercise id="sp6-e5" title="哪些東西換平台要重寫, 哪些不用" kind="paper" level="進階">
你要把一套跑在 NVIDIA 上、用了 continuous batching + overlap scheduler + chunked prefill + CUDA Graph 的推論服務移植到 AMD MI300X。 (a) 上述四項哪些屬於「排程層」邏輯、可以直接沿用? (b) 哪些需要換成 AMD 對應物? (c) 底層 attention/GEMM kernel 怎麼辦? (d) 為什麼這種「排程層共用、kernel 層分家」的切分是好的工程設計?

<Solution>
(a) **Continuous batching、overlap scheduler、chunked prefill** 都是排程層邏輯 (決定哪些 request 進 batch、CPU/GPU 如何重疊、prefill 怎麼切), 與硬體無關, 可直接沿用 — vLLM/SGLang 的 ROCm 版正是如此。

(b) **CUDA Graph → hipGraph** (消 launch overhead 的 API 對應)。

(c) 底層 attention/GEMM/MoE kernel 換成 **AITER** 提供的 ROCm 版本 (backend 為 CK/Triton/ASM), 由框架的 kernel 抽象層在 ROCm 上選用。

(d) 排程層是 latency/throughput 的「策略」, kernel 層是「執行」。 把兩者解耦後, 高階優化 (batching、overlap) 一次寫、跨平台重用; 低階優化 (kernel pipeline、MFMA vs WGMMA) 各平台獨立最佳化。 這正是本 track 的核心觀點: 同一個 overlap idea 在不同層級各自實作, 彼此不綁死。
</Solution>
</Exercise>

<Quiz
  questions={[
    {
      question: 'Continuous batching 相對 static batching 的核心改進是?',
      options: ['提高數值精度', '以 token step 為單位動態進出 request, 讓 batch 保持滿載而不被最長的 request 卡住', '減少模型大小', '關閉 KV cache'],
      answer: 1,
      explanation: '已完成的 request 立即離開、等待的立即補進, GPU 每個 forward 都算有效 token, throughput 大增。',
    },
    {
      question: 'SGLang overlap (zero-overhead) scheduler 的做法是?',
      options: ['把模型量化', '讓 scheduler 領先一個 batch, GPU 跑 batch n 時 CPU 已在準備 batch n+1 的 metadata', '用更多 GPU', '停用 continuous batching'],
      answer: 1,
      explanation: '把「CPU 排程」與「GPU forward」重疊成 2-stage pipeline, 藏住 CPU 開銷; 與 host–device stream overlap 同構。',
    },
    {
      question: 'Chunked prefill 解決的主要問題是?',
      options: ['權重太大', '長 prompt 的整包 prefill 卡住同 batch 其他 request 的 decode (head-of-line blocking)', 'GPU 數量不足', '精度不足'],
      answer: 1,
      explanation: '把長 prefill 切成小塊並與 decode 混批, 避免 decode inter-token latency 被長 prefill 拖垮。',
    },
    {
      question: 'TensorRT-LLM 的 "in-flight batching" 對應本章的哪個概念?',
      options: ['pipeline parallelism', 'continuous batching (以 token step 動態進出 request)', 'tensor parallelism', 'KV cache 量化'],
      answer: 1,
      explanation: 'in-flight batching 是 continuous batching 的 NVIDIA 命名, 兩者是同一件事。',
    },
    {
      question: '把 NVIDIA 推論服務移植到 AMD ROCm 時, 通常「不需要重寫」的是?',
      options: ['底層 attention/GEMM kernel', 'CUDA Graph 呼叫', 'continuous batching / overlap scheduler 等排程層邏輯', 'MFMA 指令'],
      answer: 2,
      explanation: '排程層邏輯與硬體無關可沿用; 換掉的是底層 kernel (→AITER/CK) 與 graph API (CUDA Graph→hipGraph)。',
    },
  ]}
/>

<Callout variant="insight" title="整個 Track 的收束">
從 kernel 內的 double buffering、host–device 的 stream、PyTorch 的 data prefetch、跨 GPU 的 1F1B, 到 SGLang 的 overlap scheduler — 全部是同一句話: 找出兩個用不同資源的 stage, 讓它們同時忙。 換了層級、換了 stage 的身分, 但 fill/steady/drain、bubble、stage 平衡、producer–consumer 這套語言一路通用。
</Callout>

<FurtherReading
  links={[
    { citation: 'L. Zheng et al., "SGLang: Efficient Execution of Structured Language Model Programs," NeurIPS 2024.', href: 'https://arxiv.org/abs/2312.07104' },
    { citation: 'LMSYS Org, "SGLang v0.4: Zero-Overhead Batch Scheduler, Cache-Aware Load Balancer," 2024.', href: 'https://lmsys.org/blog/2024-12-04-sglang-v0-4/' },
    { citation: 'W. Kwon et al., "Efficient Memory Management for Large Language Model Serving with PagedAttention (vLLM)," SOSP 2023.', href: 'https://arxiv.org/abs/2309.06180' },
    { citation: 'A. Agrawal et al., "Sarathi-Serve: Taming Throughput-Latency Tradeoff in LLM Inference with Chunked Prefills," OSDI 2024.', href: 'https://arxiv.org/abs/2403.02310' },
    { citation: 'NVIDIA Corporation, "TensorRT-LLM: In-flight Batching and Chunked Context," 2024.', href: 'https://nvidia.github.io/TensorRT-LLM/' },
    { citation: 'AMD ROCm, "AITER: AI Tensor Engine for ROCm," 2025.', href: 'https://github.com/ROCm/aiter' },
    { citation: 'AMD ROCm, "ROCm Becomes a First-Class Platform in the vLLM Ecosystem," 2026.', href: 'https://rocm.blogs.amd.com/software-tools-optimization/vllm-omni/README.html' },
  ]}
/>
