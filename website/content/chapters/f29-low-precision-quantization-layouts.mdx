# Low-Precision、Quantization 與 Layout Conversion

現代 GPU 的最高吞吐通常不在 fp32 scalar FMA, 而在 Tensor Core / Matrix Core 的 fp16、bf16、tf32、fp8、int8 路徑。 低精度優化不是把型別改小而已; 它同時牽涉 scale、accumulator、layout、alignment、epilogue 和 numerical validation。

## 低精度格式的角色

常見格式可粗分:

- **fp32**: reference、accumulator、敏感 reduction。
- **tf32**: NVIDIA 上讓 fp32 GEMM 走 Tensor Core 的折衷, mantissa 較短但 exponent 範圍接近 fp32。
- **fp16**: bandwidth 小、Tensor Core 快, 但 dynamic range 較窄。
- **bf16**: mantissa 較短, exponent 範圍接近 fp32, training 較穩。
- **fp8**: 需要 per-tensor / per-channel / per-block scaling, 適合大型 GEMM 和 attention。
- **int8 / int4**: 推論常用, 需要 zero point、scale、packing 和 dequantization。

選格式時先問: 瓶頸是 bandwidth、compute、還是 memory capacity? 如果 kernel 仍被 scalar overhead 或不規則存取限制, 換成 fp8 不會自動變快。

<Callout variant="insight" title="落到真實場景: 把 70B 模型塞進一張卡">
你要在單張 80GB H100 上服務 Llama-3 70B。 fp16 權重就要 140GB, 直接放不下; 換成 fp8 (E4M3) 權重降到 70GB 剛好塞進去, weight bandwidth 也砍半, decode 階段的 token latency 因此改善。 但這裡的瓶頸判斷很關鍵: prefill 是 compute-bound, fp8 的 Tensor Core 吞吐真的有幫助; decode 是 memory-bound 且 batch 小, 真正省的是「權重從 HBM 搬進來的 bytes」而不是 MMA。 若你誤以為 decode 慢是算力不夠而去堆 fp8 MMA tile, 只會發現沒效果 — 該做的是 weight-only int4 (如 AWQ/GPTQ) 把 bytes 壓更低。
</Callout>

## Accumulate Higher Than You Store

低精度輸入通常用較高精度累加:

```cpp
// Conceptual pattern: low-precision input, fp32 accumulation, low-precision output.
half a = A[i];
half b = B[i];
float acc = __half2float(a) * __half2float(b);
// Reduce acc in fp32, then cast or quantize in the epilogue.
```

GEMM 中常見路徑是 fp16/bf16/fp8 input, fp32 accumulator, epilogue 再轉 fp16/bf16/fp8。 Reduction、softmax、normalization 的 sum/max 通常也要留在 fp32, 尤其是長 row 或大 batch。

<Callout variant="warn" title="落到真實場景: fp16 softmax 分母溢位成 Inf">
在 fp16 attention 裡直接把 exp 的結果累加成 fp16 分母, 是 flash-attention 出現前很常見的 bug。 序列一長 (例如 seqlen 4096), 未減 max 的 `exp(score)` 動輪就爆 fp16 上限 65504, sum 變成 `Inf`, 相除得到 `NaN`, loss 直接 blow up。 修法有兩層: online-softmax 先減 row max 保證指數不溢位, 且 running sum 與 max 一律用 fp32 accumulator, 只有最後的 output 才 cast 回 fp16/bf16。 同樣道理也適用 LayerNorm 的 variance 累加 — 長 hidden dim 下 fp16 accumulate 會系統性低估。
</Callout>

## Scale 的粒度

Quantization 需要 scale, scale 粒度是效能與精度的交換:

- **Per-tensor**: metadata 最少, 但 outlier 會浪費整個 tensor 的 range。
- **Per-channel**: conv/linear 常用, 精度好很多, metadata 還可接受。
- **Per-token / per-row**: LLM activation 常用, 適合 row-wise GEMM。
- **Per-block**: fp8/int4 常用, 每 16 到 128 個元素一個 scale, 精度佳但 epilogue 較複雜。

Scale load 也要 coalesced 且 cache-friendly。 很多 int4/fp8 kernel 慢在 scale 和 zero point 的不規則讀取, 不是慢在 MMA。

<Callout variant="insight" title="落到真實場景: LLM activation 的 outlier channel">
把 OPT/Llama 這類 LLM 的 activation 量化成 int8 時, 你會發現少數幾個 channel 的絕對值比其他大 10 到 100 倍 (經典 outlier feature)。 用 per-tensor scale, 這些 outlier 會撐大整個 tensor 的 range, 其餘正常 channel 的有效位數被壓到剩幾格, 精度崩掉 — 這正是 LLM.int8() 觀察到的現象。 實務解法是換粒度: 對 activation 走 per-token (per-row) scale、對 weight 走 per-channel scale, 或用 SmoothQuant 把 outlier 的 scale 從 activation 搬一部分到 weight。 選 per-token 還有一個系統上的好處: scale 剛好對齊 row-wise GEMM 的 tile, 每個 row 讀一個 scale 就好。
</Callout>

## Packing 與 Vectorized Conversion

低 bit-width 格式必須 packing:

```cpp
// Pack two signed int4 values into one byte. Values are assumed clipped to [-8, 7].
__device__ unsigned char pack_i4(int lo, int hi) {
    unsigned ulo = static_cast<unsigned>(lo) & 0x0f;
    unsigned uhi = static_cast<unsigned>(hi) & 0x0f;
    return static_cast<unsigned char>(ulo | (uhi << 4));
}
```

Packing kernel 本身要避免成為瓶頸:

- 一個 thread 處理多個元素, 用 vectorized load/store。
- 盡量 fuse clamp、round、scale、pack。
- 避免每個元素都做 expensive division, 預先算 reciprocal scale。
- 保持輸出對齊, 讓後續 GEMM 能用 16-byte 或更寬的 transaction。

## Layout Conversion 是 Kernel

Tensor Core / MFMA 常需要特定 layout。 模型系統裡常見的隱性成本是:

- Row-major activation 轉成 tiled layout。
- Weight prepack / interleave。
- KV cache page layout conversion。
- fp16/bf16/fp8 之間的 cast。
- int4 unpack + dequant 在 GEMM 主迴圈或 epilogue 中發生。

Weight 是靜態時, prepack 一次最划算。 Activation 每步都變, layout conversion 必須 fuse 進 producer 或 consumer, 否則會多一次完整 memory round trip。

<Callout variant="insight" title="落到真實場景: vLLM 的 paged KV cache layout">
LLM 推論的 KV cache 用 paged layout (vLLM 的 PagedAttention) 把每個 sequence 的 K/V 切成固定大小的 block, 存在非連續的 page 裡。 attention kernel 讀 KV 時面對的就是「邏輯上連續、實體上分散」的 layout conversion 問題: 如果你先把 page gather 成連續 buffer 再算, 就多付一次完整的 HBM round trip; 正確做法是讓 attention kernel 直接吃 block table, 在主迴圈裡按 page 索引 load, 把 layout 轉換 fuse 進 consumer。 Weight 那側則相反 — 量化好的 int4 weight 在 load model 時就 prepack 成 MMA 友善的 interleaved layout 存起來, 每個 request 重複用, 絕不在 forward 裡重排。
</Callout>

## Epilogue Dequant / Requant

低精度 GEMM 的 epilogue 常做一串工作:

```cpp
float y = alpha * acc + bias[col];
y = gelu(y);
float q = nearbyintf(y * inv_scale[col]);
q = fminf(127.0f, fmaxf(-128.0f, q));
C_q[row * ldc + col] = static_cast<int8_t>(q);
```

把 bias、activation、scale、clamp 和 cast 放進 epilogue 可省下中間 tensor 寫回。 代價是 epilogue register pressure 變高, 且 scale/bias load 需要良好 locality。

<Callout variant="insight" title="落到真實場景: int8 CNN 的 conv-bias-relu-requant 一條龍">
量化推論框架 (TensorRT、TFLite) 對一個 `conv → bias → relu → 再量化成 int8` 的序列, 不會產生三個 kernel 各寫一次中間 tensor, 而是全部塞進 GEMM/conv 的 epilogue: accumulator 還在 register (fp32/int32) 時就加 bias、做 relu clamp、乘上 output 的 inv_scale、round、clamp 到 `[-128, 127]`、cast 成 int8 直接寫出。 這省掉兩趟 activation 的 HBM 讀寫, 對 memory-bound 的 pointwise 尾巴幫助最大。 要留意的是 per-channel 的 scale/bias 向量要能好好放進 register 或走 broadcast, 否則 epilogue 反被 scale load 拖慢, 就是前一節說的「慢在 scale 不是慢在 MMA」。
</Callout>

## Validation 不只看平均誤差

低精度 kernel 至少要測:

- Max absolute error、relative error、ULP 或 task-level metric。
- Outlier input、極小 scale、極大 scale、NaN/Inf policy。
- Deterministic vs. stochastic rounding 的差異。
- 不同 batch/sequence shape 下的誤差分佈。
- 與 library reference 在相同 accumulator 和 math mode 下比較。

<Callout variant="warn" title="不要把 dtype 當成單純效能開關">
低精度加速成立的前提是資料對齊、layout 合適、MMA 路徑真的被使用、scale metadata 不拖累 memory、且 numerical error 在產品指標內。 缺任何一項, 都可能只是更快地算錯。
</Callout>

<LabBox path="kernels/03-gemm">
在 SGEMM lab 外加一個 quantized epilogue experiment: fp32 accumulator 加 bias 後輸出 int8, scale 分別用 per-tensor、per-row、per-column。 記錄 kernel time、scale load pattern、以及相對 fp32 reference 的最大誤差。
</LabBox>

<Quiz
  questions={[
    {
      question: '低精度 GEMM 常見的 accumulator 是什麼?',
      options: ['永遠和 input 同精度', '通常比 input 高, 常見為 fp32', '只能 int32', '不需要 accumulator'],
      answer: 1,
      explanation: '低精度 input 搭配較高精度 accumulator 能保留數值穩定性, epilogue 再 cast 或 quantize。',
    },
    {
      question: 'Per-block scale 的主要代價是?',
      options: ['完全不能向量化', 'Metadata 和 epilogue 較複雜, scale load 可能成為瓶頸', '一定比 per-tensor 不準', '不支援 GEMM'],
      answer: 1,
      explanation: '粒度越細精度通常越好, 但需要更多 scale metadata 與更複雜的 load/indexing。',
    },
    {
      question: 'Weight prepack 為什麼常划算?',
      options: ['Weight 通常靜態, 可一次轉成 MMA 友善 layout 後重複使用', 'Prepack 可以增加 HBM 容量', 'Prepack 會自動提高 clock', '只為了讓程式碼好看'],
      answer: 0,
      explanation: '靜態 weight 的 layout conversion 可以離線或初始化時做, 避免每次 inference 重複付費。',
    },
    {
      question: '低精度 validation 最不應該只看哪個指標?',
      options: ['Max error', 'Outlier cases', '平均誤差', 'Task-level metric'],
      answer: 2,
      explanation: '平均誤差可能掩蓋 outlier、overflow、scale 失效和 tail shape 問題。',
    },
  ]}
/>

<FurtherReading
  links={[
    { citation: 'P. Micikevicius et al., "Mixed Precision Training," in Proc. ICLR, 2018', href: 'https://arxiv.org/abs/1710.03740' },
    { citation: 'B. Jacob et al., "Quantization and Training of Neural Networks for Efficient Integer-Arithmetic-Only Inference," in Proc. CVPR, 2018', href: 'https://arxiv.org/abs/1712.05877' },
    { citation: 'NVIDIA Corporation, "Floating Point and IEEE 754 Compliance for NVIDIA GPUs," CUDA Documentation, 2026', href: 'https://docs.nvidia.com/cuda/floating-point/index.html' },
    { citation: 'NVIDIA Corporation, "Warp Matrix Functions," in CUDA C++ Programming Guide, 2026', href: 'https://docs.nvidia.com/cuda/cuda-c-programming-guide/index.html#warp-matrix-functions' },
  ]}
/>
